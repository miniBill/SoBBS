using System.Drawing;
using System.Windows.Forms;

namespace Sobbs.Cui.Forms
{/*
    public class FormListView : FormWidget, IListView
    {
        private readonly ListView _listView = new ListView();

        public FormListView(ListViewInfo info)
            : base(info)
        {
            SuspendLayout();
            _listView.Location = new Point(0, 0);
            _listView.Size = new Size(100, 100);
            _listView.BackColor = Color.WhiteSmoke;
            Controls.Add(_listView);
            ResumeLayout(false);
        }
    }*/
}